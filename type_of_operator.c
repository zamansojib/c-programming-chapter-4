#include<stdio.h>
int main(void){

    // There are mainly three types of operator available
    // -Unary
    // -Binary
    // -Ternary

    // Unary operators: Only have one operand.Ex: ++,--
    // What is Operand?
    // a + b , here + is operator, a and b is operand
    // Binary Operator: have two operand
    // There are different type of binary operator
    // -Arithmatic (+,-,*,/,%)
    // -Relational (>,<,>=,<=,==,!=)
    // -Logical (&&,||,!)
    // -Assignment (=,+=,-=,*=,/=,%=)
    // -Bitwise (&,|,!)
    // Ternary Operator:Have three operand.Ex: conditional operator


    return 0;
}